# Data and outputs (data-derived, not committable)
data/
fit_summaries/
slides/

# Python environment and cache
.venv
__pycache__/
.ruff_cache

# R environment and session files (.Rprofile is tracked: it holds the renv
# activation line; renv/ has its own .gitignore for the project library)
.Rhistory
.RData
.Rproj.user/

# Claude Code config (keep as-is)
.claude
